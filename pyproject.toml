[project]
name = "chat-service-ai-frontend"
version = "0.1.0"
description = "Streamlit frontend"
requires-python = ">=3.11"

dependencies = [
    "httpx>=0.28.1",
    "python-dotenv",
    "requests",
    "streamlit",
]

[tool.uv]
package = false